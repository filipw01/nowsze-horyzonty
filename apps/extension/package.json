{
  "name": "@nowsze-horyzonty/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-extension.mjs",
    "package": "npm run build && node scripts/package-extension.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@nowsze-horyzonty/shared": "^0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
